{
  "name": "marque-publishing-site",
  "version": "1.0.0",
  "private": true,
  "description": "Marque Publishing website — static site with a light include-based build.",
  "scripts": {
    "build": "node build/build.js",
    "deploy": "node scripts/deploy-guard.js",
    "deploy:check": "node scripts/deploy-guard.js --check"
  }
}
